# LLM for transaction parsing
OPENAI_API_KEY=your_key_here

# OCR — Azure AI Vision (Read API)
AZURE_VISION_KEY=your_key_here
AZURE_VISION_ENDPOINT=your_endpoint_here

# STT for voice secondary mode (Whisper via OpenAI — reuses OPENAI_API_KEY above, remove this line if using a different STT service)